# ffind
ffind is an arch Linux tool to quickly find all types of specified file types in specified folders and copy them with the same structure.

```git clone https://github.com/<user>/<repo>.git && cd <repo> && makepkg -si```
